import React from "react";

const Button: React.FC = () => {
    return (
        <div className="text-center">
            <button className="btn btn-success text-white">詳細ページ</button>
        </div>
    );
}

export default Button;
